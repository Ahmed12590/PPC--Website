import React from "react";
import "./style.css";

const Banner = ({ title }) => {
  return (
    <main className="first-wrapper">
      <h1>{title}</h1>

   
    </main>
  );
};

export default Banner;
